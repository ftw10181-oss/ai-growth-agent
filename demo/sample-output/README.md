# Sample run

This frozen example makes the portfolio reviewable without a Dify account or API key. It uses the same response shape as the live workflow and is rendered by the frontend when the backend runs in mock mode.

The content is a set of hypotheses derived from the supplied brief. It is not presented as verified market research.

